В подобных файлах будет писаться дз

1. Вручную забить массив из 5 элементов вашими любимыми числами.
   Вывести каждый элемент массива на экран

2.
    1. Дана математическая функция f(x) = x ^ 2 - x * 3 - 1
       (x ^ 2 это x в квадрате)
       Создайте массив из 100 элементов. Заполните его значениями
       функции f при x от 0 до 99 (включительно).
       Распечатайте массив используя for
    2. Напишите цикл for который найдет максимальный элемент массива
       (Подумайте над алгоритмом сами,
        он заключается в сравнении каждого числа с текущим максимальным)
    3. КопиПастните 2ой пункт. Модифицируйте его так, чтобы находился МИНИМАЛЬНЫЙ элемент
    
    4*. Модифицируйте программу так, чтобы x ^ 2 - x * 3 - 1 вычислялось как функция от x
        (a[i] = f(x)), (создайте функцию f, которая будет возвращать x ^ 2 - x * 3 - 1)
