1. Модернизируйте прогу с прошлой пары. 
   Вынесите нахождение максимума и минимума в отдельные функции.
   Для этого следуйте следуюшим простым шагам:
       I. Создайте "декларатор функции"
           a. Обдумайте что должна возвращать функция max?
              Это и будет первым словом в деклараторе функции
           b. Назовите функцию
           c. Обдумайте аргументы, функция должна работать с массивами
              произвольной длины.
       II. Распишите логику функции.
           По сути, мы это уже сделали, просто копипастните код в функцию,
           не забывая о том, что теперь у нас массив длины len
       III. Вызовите функцию в коде (это уже сделано, вам только раскомментить)

2**. Напишите свою первую игру
    Распишу чуть позже
